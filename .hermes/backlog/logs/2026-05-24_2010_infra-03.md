# Log infra-03 — Backups TimescaleDB

**Timestamp** : 2026-05-24 20:10 UTC
**Tâche** : Mettre en place les backups automatisés TimescaleDB
**Statut** : ✅ SUCCÈS

## Actions précises

1. **Correction héritage** : L'agent délégué avait créé un dossier `/opt/noisyless/scripts/backup.sh/` au lieu du fichier script. Supprimé, recréé proprement.
2. **Script créé** : `/opt/noisyless/scripts/backup.sh` (874 octets, permissions 774)
   - `docker compose exec -T timescaledb pg_dump -U noisyless noisyless` → fichier horodaté
   - Rétention 30 jours via `find -mtime +30 -delete`
   - Logging dans `backup_script.log`
3. **Test d'exécution** : Lancé manuellement → backup OK (21 205 octets, fichier `backup_20260524_2010.sql`)
4. **Cron installé** : `0 3 * * * /opt/noisyless/scripts/backup.sh >> /opt/noisyless/backups/backup.log 2>&1`
   - Doublons nettoyés (l'agent avait ajouté la ligne 2 fois)
   - Cron final : 2 entrées (SSL renewal + backup)
5. **Vérification** : `crontab -l` OK, script exécutable, backup.sql non vide

## Fichiers modifiés/créés

- `/opt/noisyless/scripts/backup.sh` — créé
- `/opt/noisyless/backups/backup_20260524_2010.sql` — 21 Ko
- `/opt/noisyless/backups/backup_script.log` — log de test
- Crontab utilisateur `noisyless` — modifié

## Erreurs rencontrées

- Aucune bloquante
- Warnings non critiques : `version` obsolete dans docker-compose.yml, `circular foreign-key constraints` sur continuous_agg (normal TimescaleDB)
