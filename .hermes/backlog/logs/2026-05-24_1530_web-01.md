# Log — web-01 : Formulaire d'achat rapide dans index.html

**Timestamp** : 2026-05-24 15:30 (estimé)
**Tâche** : Ajouter le formulaire d'achat (bouton Acheter) dans web/site/index.html
**Agent** : Hermes-MBHREP (orchestrateur backlog NOISYLESS)
**Statut** : ✅ Succès

## Actions précises

1. **Lu** `~/.hermes-mbhrep/backlog/todo.json` → détecté web-01 comme première tâche exécutable (stripe-01 et stripe-02 bloquées car nécessitent l'intervention humaine pour la clé Stripe LIVE)
2. **Marqué** `stripe-01` et `stripe-02` comme `blocked` avec raison explicite
3. **Marqué** `web-01` comme `in_progress`
4. **Lu** `NEWTODO.md` section 1.3 pour les spécifications du formulaire
5. **Modifié** `/home/mathieu/Kdrive/01_CLIENTS/NOISYLESS/web/site/index.html` :
   - **Ajouté** un formulaire `#quick-checkout-form` dans la carte produit NOISYLESS Simple (lignes 220-227)
     - Champs : email (`#quickEmail`), nom (`#quickName`)
     - Bouton "Acheter — 79€" avec style cohérent au design existant
     - Formulaire inline dans une `border-t` séparée visuellement
   - **Ajouté** la fonction JavaScript `startCheckout(e)` avant la fonction `checkout()` existante (lignes 494-536)
     - Appelle `POST /shop/checkout` sur `platform.noisyless.com`
     - Gère les deux formats de réponse : `session.url` (redirection directe) ou `session.session_id` (via Stripe.js)
     - Gestion d'erreur avec `alert()` et restauration du bouton
     - URL success : `/merci`, cancel : `/annule`
6. **Marqué** `web-01` comme `done`

## Fichiers modifiés

- `~/Kdrive/01_CLIENTS/NOISYLESS/web/site/index.html` : +46 lignes (formulaire HTML + fonction JS)

## Résultat

✅ Le formulaire d'achat rapide est inséré dans la carte produit principale. La fonction JS est prête à être testée dès que l'API Stripe sera fonctionnelle (stripe-01 à débloquer).

## Notes

- Le formulaire utilise les URLs `/merci` et `/annule` (tâches web-02 et web-03 à venir)
- Dépend indirectement de stripe-01 pour être pleinement fonctionnel
- Le formulaire existant `#shopForm` dans la section `#shop` reste intact et fonctionnel (checkout complet avec produits et abonnements)
