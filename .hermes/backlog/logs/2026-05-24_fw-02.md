# Log — fw-02 — 2026-05-24

## Tâche
**ID** : fw-02  
**Goal** : Créer le firmware node-radar-60ghz complet  
**Priorité** : 3  

## Statut final : DONE

## Résumé
Firmware ESP32-S3 complet créé pour le capteur radar MS72SF1 (60 GHz).

## Fichiers créés/modifiés (18 fichiers)

### Composant ms72sf1 (driver capteur radar)
- components/ms72sf1/ms72sf1.h — API : init(), read_frame(), deinit()
- components/ms72sf1/ms72sf1.c — UART 115200, parser header+TLV, targets float32
- components/ms72sf1/CMakeLists.txt

### Composant mesh (ESP-NOW)
- components/mesh/mesh.h — Types MESH_TYPE_DATA_RADAR, mesh_radar_target_t
- components/mesh/mesh.c — mesh_init(), send_radar_payload(), recv callback
- components/mesh/CMakeLists.txt

### Composant config (NVS)
- components/config/config.h — identity, calib, sensor, power configs
- components/config/config.c — NVS read/write all configs
- components/config/CMakeLists.txt

### Composant mqtt (publication master)
- components/mqtt/mqtt.h — mqtt_init(), publish_heatmap(), deinit()
- components/mqtt/mqtt.c — ESP-MQTT client, topic noisyless/heatmap
- components/mqtt/CMakeLists.txt

### Application main
- main/main.c — Déstubé complet : sensor_task (ms72sf1 → mesh), master_task (collect → JSON → MQTT), health_task (battery LED), app_main (init sequence). 0 TODO.

### Configuration PlatformIO
- platformio.ini — ESP32-S3, ESP-IDF framework
- partitions.csv — nvs + factory
- sdkconfig.defaults — ESP-NOW enabled

## Fichiers inchangés
- CMakeLists.txt (racine)
- main/CMakeLists.txt

## Architecture
```
ESP32-S3 @240MHz
├── UART2 (115200) → MS72SF1
├── Slave mode : radar_read → parse_TLV → ESP-NOW broadcast
├── Master mode : collect_all_nodes → JSON fusion → MQTT publish
├── Health : ADC battery monitor, LED indication, deep sleep if critical
└── Config : NVS persistence (identity, calibration, sensor, power, mesh)
```

## Payload MQTT (format cible)
Schema nl.telemetry.v1, model nl-map-out, targets[] avec x/y/z/vx/vy/q, health intégré.
